<p id="notice"><%= notice %></p>

<section class="container">
<div class="row">
  <div class=class="col-xs-12 visible-xs">
    <% @titles.each do |title| %>
          <%= image_tag(title.picture_url, class: "img-rounded img-responsive") if title.picture.present? %>
          <% hoge = title.author %>
                <%= hoge.html_safe %>
    <% end %>
  </div>
</div>
</div>
<br>

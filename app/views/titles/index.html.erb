<p id="notice"><%= notice %></p>

  <tbody>
    <% @titles.each do |title| %>
      <tr>
        <p>
          <%= image_tag(title.picture_url, class: "img-rounded img-responsive") if title.picture.present? %>
          <%= link_to "元のtweet", title.memo, :target => "_blank" %>
        </p>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
